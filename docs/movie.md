# 🎬 Кино

## Назначение
Раздел «🎬 Кино» открывается прямо из главного меню короткой витриной: недельный локальный прокат, именинник и связанный с показанным кино факт. Персональный подбор запускается кнопкой `🍿 Что посмотреть`.

## Как это работает

Названия блоков «Что в кино», «Именинник дня» и «Интересно» выделяются жирным. Ребусов нет; факт относится к фильму, сериалу или создателю из текущей витрины.

### Что посмотреть
При входе в «🎬 Кино» бот показывает витрину дня. `🍿 Что посмотреть` открывает персональную карточку, а первая кнопка `✨ Подобрать новую премьеру` — общую перелистываемую витрину фильмов и сериалов с постерами.

### Анализ вкуса (на лету)
Перед каждой рекомендацией бот заново анализирует вкус пользователя по его спискам:
любимые фильмы и сериалы, просмотренные, отклонённые («Не нравится»),
недавно показанные. Каждый любимый проект резолвится в TMDb, и из его данных
собираются приоритеты: любимые жанры, страны, повторяющиеся режиссёры, преобладание
фильмов или сериалов, средний рейтинг, современные или старые проекты. Это **приоритеты, а
не жёсткие ограничения** — они влияют на порядок, но не запрещают другое. Поскольку профиль
пересобирается каждый раз, любое действие (добавил в любимое или отклонил) сразу
меняет будущие рекомендации — вкус «обучается» без ручной настройки.

### Источник рекомендаций — TMDb
Для каждого любимого проекта бот берёт из TMDb списки **Recommendations** и **Similar**
(для фильмов и сериалов свои эндпоинты). Это основной источник новых названий. Кандидаты
со всех любимых объединяются в общий пул; если один и тот же проект рекомендован сразу от
нескольких любимых, он получает приоритет («часто рекомендуют вместе»). ИИ подключается
только как запасной вариант, если данных TMDb недостаточно (например, список любимых пуст
или сервис недоступен), а также для объяснения причин.

### Фильтрация и порог рейтинга
Из выдачи всегда исключаются: уже просмотренное, любимое, отклонённое,
недавно показанное и дубликаты. Кандидаты до 2000 года и с рейтингом TMDb ниже 7.0
отсекаются. Рейтинг учитывается только при выборке не менее 50 голосов; взрослый контент
исключается. Для
кандидатов из слабого источника Similar дополнительно требуется заметное совпадение жанров.

### Ранжирование
Оставшиеся кандидаты сортируются по совпадению со вкусом: жанры, страны, повторяющиеся
режиссёры, преобладающий тип, рейтинг с числом голосов, популярность TMDb и частота появления от разных любимых.
Первый в списке и показывается.

## Карточка
Карточка содержит: постер, название (и оригинальное), отдельно подписанную дату выхода, тип (фильм/сериал), жанры,
рейтинг TMDb и описание. Персональная причина влияет на выбор, но строка
`Потому что вам понравился…` пользователю не показывается. Для менее надёжного Similar
показываются только подтверждённые общие жанры — формулировка «Похоже на…» не используется.

Для сериалов дополнительно показываются: число сезонов и серий (📊), статус
(🎥 Продолжается / ✅ Завершено), дата следующей серии (⏳ Следующая серия — …) или
«⏳ Новый сезон ожидается», примерная длительность серии.

Для фильмов: длительность (⏱️), страна (🌍) и студия (🎬), если известна.

### Сейчас в кино
Блок «🎟️ Идёт в кино» в разделе Кино показывает до пяти известных фильмов, которые
подтверждены в локальном кинотеатральном прокате. Регион берётся из профиля
пользователя, а не задаётся вручную. Список не зависит от любимых фильмов и не влияет на вкус:
это просто быстрый обзор текущего проката. В витрину попадают только фильмы с достаточной
внутренней проверкой качества и подтверждённой короткой завязкой. Фильм без описания
не показывается на главном экране; при временно неполных данных сохраняется последняя
подходящая афиша прошлой недели, а без неё блок честно остаётся пустым.
В блоке «Что в кино» показаны только три самых популярных фильма из подтверждённого городского проката.
Каждый фильм — одна короткая строка `• «Название» (жанр) · Короткая завязка.`; название ведёт на точный YouTube-трейлер,
а предпросмотр ссылки на этом экране отключён. Рейтинг пользователю не выводится.

Ребус и факт меняются каждый день без повторов внутри месяца. Для Кино хранится 31 проверенная редакторская карточка; при первом открытии месяца её календарная последовательность сохраняется в общем Railway-кэше. Новый месяц получает другой сдвиг порядка, а AI для ребусов не вызывается. Ответ ребуса скрыт нативным spoiler-форматированием
Telegram. Сохранённые названия, ответы и факты очищаются от случайного Markdown, HTML-entities и битых символов перед показом.
Именинник и одна его заметная работа проверяются через Wikidata один раз в сутки и кэшируются глобально;
при временной недоступности источника бот не подставляет выдуманную дату рождения.

Список фильмов главной витрины кэшируется на неделю с понедельника и планово прогревается раз в неделю. Ежедневные ребус и именинник меняются отдельно и не пересобирают прокат. Вход в раздел не запускает персональный подбор. Если новая недельная афиша не дала ни одного подходящего фильма, витрина сохраняет последний непустой список прошлой недели. Более старый список не используется. По кнопке подбора остаётся локальный проверенный вариант.

При подборе другого фильма или выборе жанра главный экран не заменяется
служебным сообщением. Под ним временно остаётся одна вертикальная inline-кнопка
`🎬 Ищу кино...`, затем `🎯 Учитываю предпочтения...` и `📝 Готовлю карточку...`; после завершения новая карточка приходит отдельным
сообщением с постером, если он есть в TMDb, а кнопки старого экрана восстанавливаются.

В общей ротации рекомендаций бот старается показывать минимум один проверенный
ЛГБТ-фильм, мультфильм или сериал на каждые пять карточек. Проект проходит обычные
настройки типа и минимального рейтинга. Только тайтлы из проверенного списка получают
пометку `🏳️‍🌈 ЛГБТ`; AI не определяет тематику по догадке. Если подходящего проекта
нет, обязательная попытка переносится на следующую рекомендацию.

## Кнопки

Главный экран — витрина с недельным прокатом и кнопками `✨ Подобрать новое кино`, `🎟️ Премьеры фильмов`, `📺 Премьеры сериалов` и `🎚️ Моё кино`. Отдельных вкладок `Топ-5 фильмов` и `Топ-5 сериалов` нет. Экран премьер показывает период без служебной подписи о количестве или популярности; постеры переключаются кнопками `◀️ 1/5 ▶️`, а `⬅️ Назад | #️⃣ Главная` находятся под той же карточкой. Экран `🎚️ Моё кино` оформлен как шкаф: жирные названия жанров, под каждым — русские названия сохранённых фильмов и сериалов через запятую, между жанрами пустая строка. Используются шесть основных жанров в едином порядке: Комедия, Ужасы, Фантастика, Триллер, Романтика, Драма. Кнопка жанра сразу открывает карточку первого проекта с постером, годом и коротким описанием; остальные переключаются в том же сообщении кнопками `◀️ 1/5 ▶️`. Под карточкой находятся `❌ Удалить`, `🆕 Добавить фильм`, `⬅️ Назад` и `#️⃣ Главная`. Режима `✏️ Изменить` нет.
На карточке персональной рекомендации, сверху вниз:
- **✨ Другое кино** — широкая кнопка первой строкой: помечает проект как «не нравится» (в чёрный
  список) и сразу показывает другой вариант без промежуточного сообщения в чат.
- **🎭 По жанру** — вторая строка; сохраняет жанр текущего подбора.
- Внизу карточки `⬅️ Назад` возвращает в Кино, а `#️⃣ Главная` — в главное меню.

Отдельной кнопки «Уже видел» нет — она дублировала смысл «Другое кино» (обе вели к следующему
варианту).

Отклонённые, просмотренные и удалённые из списков проекты попадают в общую внутреннюю
категорию «Не рекомендовать» и не повторяются. Отдельных кнопок «Скрытое» и «Смотрел»
в категории Кино нет.

### По жанру
Быстрое меню «По жанру» — 6 самых популярных жанров в один столбец, без эмодзи рядом
с названиями: Комедия, Ужасы, Фантастика, Триллер, Романтика, Драма. Это разовый
фильтр для следующей рекомендации, а не постоянное предпочтение.

### Премьеры

Общая витрина премьер показывает фильмы и сериалы в одной перелистываемой карточке.
Если Telegram не может загрузить постер по URL, бот сохраняет экран и кнопки в
текстовом виде; при перелистывании недоступное новое изображение не ломает сценарий.

`🎟️ Премьеры фильмов` показывает фильмы, которые уже недавно вышли в прокат или выходят
в ближайшие две недели в стране пользователя. В списке остаются только фильмы
текущего года (сейчас — 2026). Бот сортирует их по популярности TMDb и оставляет максимум пять
фильмов с короткой законченной завязкой и постером. Название скрывает ссылку на точный
YouTube-трейлер, подтверждённый карточкой TMDb. Каждый постер показывается в одной
редактируемой Telegram-карточке; стрелки меняют фильм без нового сообщения. Фильм без постера
не появляется в отдельной витрине премьер; если Telegram не принял изображение, показывается
компактная текстовая версия экрана.
На главной витрине «Что в кино» тот же фильм остаётся одной компактной
строкой: `• «Название» (драма, комедия) · Короткая завязка.` Список из трёх пунктов
может показать подтверждённый фильм без постера.

### Премьеры сериалов

`📺 Премьеры сериалов` показывает до пяти премьер: новые сериалы за последний месяц и
с подтверждённой датой на ближайшие 60 дней. Сначала идут
новые сезоны сериалов из `🎚️ Моё кино`, затем новые сериалы. В витрину попадают только
проекты с рейтингом TMDb строго выше 7/10, подтверждённой датой, постером и описанием.
Карточка показывает номер сезона или пометку `Новый сериал`, дату, рейтинг и жанры;
продолжение избранного отмечается `из Моего кино`. Постеры перелистываются в том же
сообщении, а `⬅️ Назад | #️⃣ Главная` закреплены под карточкой.

Региональные даты берутся из TMDb; кэш привязан к календарной неделе и обновляется
по понедельникам ночным расписанием. Днём экран
только читает готовую витрину: при временной проблеме ночного обновления он
показывает последнюю сохранённую, не расходуя лимит TMDb. Если кэша ещё нет после запуска или смены формата, первый вход
собирает его один раз; следующие открытия снова читают кэш. Пустой или временно
недоступный источник не кэшируется как отсутствие премьер.

До трёх фильмов из этой витрины входят в пятничное уведомление
`🎲 Ближайшие события`: одна строка на фильм, название ведёт на трейлер,
описание не добавляется.

## Базовые правила подбора
Кино подбирается по любимым фильмам и сериалам из `🎚️ Моё кино`; через первую кнопку `🔣 Выбрать предпочтения` можно
выбрать фильмы или сериалы, новинки или любые годы и минимальный рейтинг. Для всех подборок
действует общий приоритет: релиз после 2000 года и рейтинг TMDb не ниже 7.0.

## Вкус и повторы
Список «🎚️ Моё кино» уточняет подбор по жанрам, странам, режиссёрам и типу проекта.
Кнопка «✨ Другое кино» исключает текущий вариант из следующих рекомендаций. Список
пополняется вручную через «🆕 Добавить фильм» внутри «🎚️ Моё кино». После ручного добавления бот показывает отдельную карточку с названием, подтверждёнными данными TMDb (если они доступны) и кнопкой возврата к списку.

Каждая запись хранится без эмодзи и Markdown в одном виде: `Название (фильм, 2024)` или
`Название (сериал, 2023)`. При добавлении и в фоновом проходе для старых списков бот
берёт русское название, тип и год из TMDb. Если источник временно не ответил, чистое
название сохраняется без выдуманных данных и уточняется при следующем проходе.

При ручном добавлении premium AI разбирает название и предлагает до трёх вариантов
с типом и годом. Пользователь выбирает нужный вариант, после чего данные проверяются
через TMDb; без выбора первый результат автоматически не сохраняется.

## Ограничения
- При пустом списке «🎚️ Моё кино» бот использует качественные варианты из TMDb; добавленные
  фильмы и сериалы сделают следующие рекомендации точнее.
- Порог рейтинга TMDb — 7.0; если подходящего варианта нет, бот предложит сменить жанр
  или повторить подбор.
- Данные (детали, постеры, рейтинги) зависят от наличия проекта в TMDb; названия из списков
  резолвятся по поиску, поэтому редкие/неоднозначные названия могут не находиться.
- Списки «не показывать снова» собираются из любимых, просмотренных, отклонённых
  и недавно показанных — при их очистке рекомендации могут начать повторяться.
- Профиль вкуса не хранит отдельные «веса» — он выводится из актуальных списков при каждой
  рекомендации.

Постеры фильмов и сериалов всегда выбираются отдельно из английской коллекции TMDb.
Язык описания карточки остаётся русским; локализованные и безъязыковые постеры не используются.
